<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
</head>
<body>
    <header>

    </header>
    <div class="main">
        <pre>
            {{ facebookDomain }}
        </pre>
        {% for key,value in facebookAccessToken %}
            <div>{{ key }} : {{ value }}</div>
        {% endfor %}
        {% for shortDomain in shortDomainList %}
            <div>
                {% include 'shortDomain.html.twig' %}
            </div>
        {% endfor %}
    </div>
    <footer>

    </footer>
</body>
</html>